import { NextResponse } from "next/server";

/** CRUD admins établissement — squelette API */
export async function GET() {
  return NextResponse.json({ message: "À implémenter" }, { status: 501 });
}

export async function POST() {
  return NextResponse.json({ message: "À implémenter" }, { status: 501 });
}

import { NextResponse } from "next/server";

/** Super Admin plans — Phase 1 — squelette API */
export async function GET() {
  return NextResponse.json({ message: "À implémenter" }, { status: 501 });
}

export async function POST() {
  return NextResponse.json({ message: "À implémenter" }, { status: 501 });
}
